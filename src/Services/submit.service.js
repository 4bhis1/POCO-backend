const fetchUserGithubCred = async (user_id) =>{

    

}

const submitService = async (req, res) => {
  const { body, user } = req;
  const { quertion, solution, platform } = body;




};

module.exports = {
  submitService,
};
